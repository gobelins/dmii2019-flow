/**
 * @constructor
 */

class Animation {

    /*
     * Shape constructor
     *
     * */
    constructor( shapesID, duration, nameAnimation ) {

    }

    /*
   * récupérer les animations
   * return camera param : objet ;
   * */
    get(time) {
        // var animationList = null;  //TODO ici on récupère les datas en fonction du path -> JSON
        // this.mapAnimation = new Map(animationList);
        // return this.mapAnimation.get(this.timeSart);

        return []

    }

    /*
     * read animation
     * return void
     * */
    read() {

    }
}

export default Animation;